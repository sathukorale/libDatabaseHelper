version: 1.0.{build}
matrix:
  allow_failures:
    - configuration: 2008
      platform: x64
      
configuration:
  - 2008
  
platform:
  - x86
  
before_build:
  - nuget restore
  
build:
  verbosity: minimal
